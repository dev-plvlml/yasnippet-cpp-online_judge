# -*- mode: snippet -*-
# name: Disjoint Set Union (by size)
# key: dsu
# --
`(with-temp-buffer
   (insert-file-contents
     (expand-file-name "c++-mode/online_judge/disjoint_set_union_by_size.h" (car yas-snippet-dirs)))
   (buffer-string))`
DSU ${1:dsu}(${2:n + 1});
